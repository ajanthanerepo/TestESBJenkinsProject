buildscript {
 	repositories {
      		jcenter()
      		mavenCentral()
    	        maven { url 'http://maven.wso2.org/nexus/content/groups/wso2-public' }
  }
}

group = 'com.example.TestESBJenkinsProject'
version = '1.0.0'

apply plugin: 'java'
apply plugin: 'maven-publish'

task buildWSO2ESBProject(type:Exec) {    
        println "Executing the build for TestESBJenkinsProject..."
	commandLine 'mvn', 'clean', 'install'
}
